name: focusflow_flutter
description: A Flutter port of the FocusFlow Routine Manager (React TSX UI).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  collection: ^1.18.0
  intl: ^0.19.0
  fl_chart: ^0.69.0
  shared_preferences: ^2.5.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
